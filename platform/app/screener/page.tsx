 "use client";
import {useEffect,useState} from "react";import {rpc} from "@/lib/supabase";import type {Investment} from "@/lib/types";
export default function Screener(){const [rows,setRows]=useState<Investment[]>([]),[q,setQ]=useState(""),[risk,setRisk]=useState("");async function run(){const d=await rpc<Investment[]>("app_search_investments",{p_search:q||null,p_risk_level:risk||null,p_sort:"return_1y_desc",p_limit:100});setRows(d||[])}useEffect(()=>{run().catch(()=>{})},[]);
return <section className="section"><div className="container"><div className="eyebrow">Screener</div><h1>Find investments that match your criteria.</h1><div className="toolbar"><input className="field" placeholder="Search symbol or name" value={q} onChange={e=>setQ(e.target.value)}/><select className="field" value={risk} onChange={e=>setRisk(e.target.value)}><option value="">All risk levels</option><option>Low</option><option>Medium</option><option>High</option></select><button className="btn primary" onClick={run}>Apply</button></div><table className="table"><thead><tr><th>Investment</th><th>Risk</th><th>1Y</th><th>MER</th><th>AUM</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td><b>{x.symbol}</b> · {x.name}</td><td>{x.risk_level??"—"}</td><td>{x.return_1y_pct?.toFixed(1)??"—"}%</td><td>{x.mer_pct?.toFixed(2)??"—"}%</td><td>{x.aum?`$${(x.aum/1e9).toFixed(2)}B`:"—"}</td></tr>)}</tbody></table></div></section>}